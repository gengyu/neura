# Neura PRD v0.3

## 1. 产品定位

Neura 是一个长期运行的个人上下文捕获与记忆运行时。

它不是从“做一个通用 Agent Runtime”开始，而是先解决一个更具体的问题：

> 用户每天产生大量零散信息，想法、截图、文件、网页、外部事件和临时任务不断散落。Neura 要把这些信息低成本接住，理解它们，判断哪些值得长期保存，并在之后可以被检索、总结、提醒或用于后续行动。

一句话：

> Neura v1 是一个无 UI 优先的个人上下文收集、记忆与回顾系统。

Runtime、插件、Agent Loop、工具和权限策略都是实现这个闭环的方式，不是第一层产品目标。

---

## 2. 目标用户

### 2.1 核心用户

Neura v1 面向个人高频信息工作者，尤其是：

- 开发者
- 独立产品开发者
- 研究型创作者
- 需要长期跟踪项目上下文的人

这些用户每天会产生大量半成品信息：

- 临时想法
- 技术决策
- 产品判断
- 截图
- 文件
- 代码片段
- 外部系统事件
- 待办和提醒

他们的问题不是“不知道怎么写笔记”，而是：

- 很多信息来得太快，来不及整理
- 信息分散在命令行、文件夹、截图、聊天、网页和自动化工具里
- 当时觉得有用，之后却很难找回
- 项目的长期决策和上下文很容易断裂
- 传统笔记工具需要用户主动打开、分类、命名、整理

### 2.2 非目标用户

v1 不优先服务：

- 需要团队知识库协作的人
- 需要完整项目管理系统的人
- 需要跨设备云同步的人
- 需要可视化笔记编辑器的人
- 需要通用自动化平台的人

这些方向可以作为未来扩展，但不应干扰 v1 的核心闭环。

---

## 3. 核心问题

Neura v1 要解决的问题是：

> 如何让用户几乎不需要整理，也能把每天产生的有价值上下文沉淀下来，并在之后用得上？

这个问题拆成四个子问题：

1. **捕获成本低**
   用户可以通过 CLI、图片、文件夹、截图目录、Webhook 等方式把信息丢给 Neura。

2. **自动理解**
   Neura 能把不同来源的信息标准化，识别场景、主题、标签、任务类型和长期价值。

3. **形成记忆**
   Neura 不保存一切，而是判断是否值得记忆，并尽量更新相似记忆，避免重复堆积。

4. **之后可用**
   用户可以搜索记忆、查询上下文、查看最近处理结果、创建提醒，并在需要时获得输出。

---

## 4. 产品原则

### 4.1 先做个人上下文闭环

v1 优先完成：

```text
输入捕获 -> 信息理解 -> 记忆沉淀 -> 检索/提醒/输出
```

如果一个功能不能明显增强这个闭环，就不应进入 v1 优先级。

### 4.2 Runtime 是内核，不是卖点

Neura 的工程形态是 Runtime，但用户真正感知到的价值是：

- 我丢进去的信息没有丢
- 它帮我整理成可用记忆
- 我之后能搜到、问到、想起来
- 它能在合适的时候提醒我

### 4.3 无 UI 优先，但不是无可视化

Neura 不以传统桌面客户端或 Web App 为核心。

但管理界面可以存在，作用是：

- 查看状态
- 查看输入、任务、输出、日志
- 搜索记忆
- 管理插件
- 处理审批
- 创建定时任务

管理 UI 是 Runtime 的观察和控制面板，不是产品本体。

### 4.4 插件只负责边界

插件只分为两类：

- 输入插件：把外部信息送进 Neura
- 输出插件：把 Neura 的结果送到外部

工具不是第三类插件。工具是 Agent Loop 在处理过程中可调用的内部能力，例如读文件、写文件、搜索记忆、发送 HTTP 请求或执行命令。

### 4.5 默认保守

Neura 可以长期运行，但不能默认拥有无限权限。

高风险动作必须走审批或明确授权：

- 执行 shell 命令
- 删除文件
- 覆盖文件
- 向外部系统写入数据
- 发送消息或邮件
- 访问敏感目录

---

## 5. v1 成功标准

Neura v1 成功，不是因为它“支持很多插件”，而是因为它能稳定完成下面的真实工作流。

### 5.1 基础验收

用户连续使用一周，向 Neura 输入：

- 20 条临时想法
- 10 张截图
- 5 个文件
- 若干条 Webhook 或自动化事件
- 若干个提醒请求

一周后，用户应该能够：

- 搜到重要想法
- 找回某张截图里的关键信息
- 看到项目相关记忆被合并或更新
- 让 Neura 总结某个主题的上下文
- 收到按时触发的提醒
- 查看哪些输入被忽略、哪些被记忆、哪些失败

### 5.2 定性标准

用户应该觉得：

- “我可以随手丢东西进去。”
- “它不会把所有垃圾都存成记忆。”
- “之后搜索时真的能找回来。”
- “它知道哪些事情更像长期上下文。”
- “遇到高风险动作时，它不会擅自执行。”

### 5.3 反标准

如果 v1 主要表现为下面这些结果，就说明方向偏了：

- 插件很多，但用户不知道每天为什么要用
- 管理界面很完整，但记忆质量差
- Agent 能调用很多工具，但信息沉淀不可控
- 输出很主动，但用户觉得吵
- 架构很抽象，但没有稳定的个人上下文闭环

---

## 6. 当前记忆层状态

Neura 目前的记忆系统已经从“简单存摘要”升级为一个可用的本地个人记忆层。

它当前更接近：

> 可用的本地混合检索记忆库，适合个人项目上下文沉淀。

但它还不是 Claude / ChatGPT Memory 那种强语义、长期画像、跨场景自适应的记忆系统。

### 6.1 当前怎么存

记忆存储在本地 SQLite：

- `data/neura.db`

核心表是 `memories`，主要字段包括：

- `content`
- `summary`
- `tags`
- `sourceInputId`
- `sourceType`
- `sourceId`
- `importance`
- `confidence`
- `createdAt`
- `updatedAt`

原始输入不会直接等同于记忆。

Agent Loop 会先完成：

```text
输入标准化 -> 模型分析 -> 长期价值判断 -> 创建 / 更新 / 跳过记忆
```

相似内容会优先更新已有记忆，避免重复堆积。

记忆需要支持用户后期维护：

- 编辑
- 打标签
- 合并
- 删除
- 导出
- 导入
- 备份

### 6.2 当前怎么检索

记忆检索已经不是简单 `LIKE` 搜索。

当前检索应采用混合检索和重排，综合考虑：

- 语义哈希向量
- 关键词 IDF
- 短语命中
- 标签命中
- 文本相似度
- 重要性
- 更新时间

CLI 和 Admin UI 应显示：

- 相关性分数
- 命中原因
- 记忆摘要
- 标签
- 更新时间

中文检索需要 n-gram 增强，避免退化成不稳定的单字匹配。

当用户查询历史问题时，Agent Loop 应先召回相关记忆，再把相关记忆作为模型上下文。

### 6.3 大输入处理策略

长文本、大文件和大文件夹不能整篇直接进入模型。

系统应采用：

- 预算内预览
- 分块索引
- 超预算 warning
- 可处理文本的摘要
- 二进制文档元信息记录

URL 默认只保存链接和用户提供的说明，不自动抓取正文。

这个策略的目标是：

- 避免几百页文件直接烧 token
- 避免大文件夹拖垮 Agent Loop
- 让用户知道哪些内容只是预览处理
- 给未来 map-reduce 深度摘要预留空间

### 6.4 当前适合的场景

当前记忆层已经适合：

- 找回产品设计想法
- 查询项目决策
- 搜索某个主题的历史上下文
- 召回标签相关记忆
- 防止重复记忆堆积
- 手动修正、合并、删除脏记忆

### 6.5 当前不足

与主流 AI 产品级长期记忆相比，当前还缺：

- 真实 embedding 模型
- 分层记忆 schema
- 长文 map-reduce 深度摘要
- 记忆衰减
- 冲突检测
- 自动遗忘
- 基于用户反馈的检索权重调优

分层记忆至少应逐步区分：

- 事实
- 偏好
- 项目
- 人物
- 任务
- 决策

下一阶段如果要明显提升记忆效果，优先级应是：

1. 接入真实 embedding
2. 设计分层记忆 schema
3. 增加长文 map-reduce 摘要
4. 增加记忆冲突和置信度管理
5. 建立用户反馈到检索权重的调优机制

---

## 7. MVP 范围

### 7.1 必须包含

v1 MVP 必须包含：

- CLI 文本输入
- CLI 图片输入
- Webhook 输入
- 文件夹监听输入
- 截图目录监听输入
- 输入标准化
- 模型结构化分析
- 记忆判断、写入和相似记忆更新
- 本地记忆搜索
- SQLite 本地存储
- CLI 输出
- 本地日志输出
- 系统通知输出
- 定时提醒
- 高风险工具审批
- Runtime 状态查看
- 插件状态查看
- 基础管理界面

### 7.2 可以延后

以下功能不进入 v1 必须范围：

- 插件市场
- 多设备同步
- 云端账号系统
- 团队协作
- 复杂可视化笔记编辑器
- 移动端 App
- 完整浏览器扩展
- 复杂工作流编排器
- 多用户权限系统
- Kubernetes / Docker Compose 部署

### 7.3 明确不做

v1 不把 Neura 定义为：

- 聊天机器人
- 通用后端服务
- 传统笔记软件
- 桌面客户端
- 自动化平台
- LangChain 或其他 Agent 框架的壳

---

## 8. 核心使用场景

### 8.1 随手记录想法

用户输入：

```bash
bun run neura -- input "Neura 的插件应该只分输入和输出两类，工具不要算插件"
```

Neura 应该：

- 识别这是产品设计想法
- 提取主题和标签
- 判断有长期价值
- 写入或更新相关记忆
- 在 CLI 中返回简短处理结果

### 8.2 捕获截图上下文

用户把截图放入截图目录，或通过 CLI 输入图片。

Neura 应该：

- 读取图片
- 提取可见文字或描述主要内容
- 判断截图属于哪类场景
- 生成摘要和标签
- 保存有价值的截图上下文

### 8.3 自动处理文件夹内容

用户把 Markdown、TXT、JSON、PDF、代码文件或图片放入 inbox 目录。

Neura 应该：

- 检测新文件
- 读取可处理内容
- 总结文件
- 提取长期上下文
- 记录处理状态

### 8.4 接收外部事件

外部系统通过 Webhook 发送事件，例如网页收藏、自动化结果或系统通知。

Neura 应该：

- 接收事件
- 标准化事件结构
- 判断是否值得记忆
- 必要时创建任务、提醒或日志

### 8.5 查询已有记忆

用户输入：

```bash
bun run neura -- memory search "插件系统"
```

Neura 应该返回相关记忆，并尽量按相关性排序。

### 8.6 创建提醒

用户输入：

```bash
bun run neura -- schedules add --mode reminder --in 30m "提醒我整理 Neura 的 PRD"
```

Neura 应该：

- 创建定时任务
- 到期后通过合适的输出插件提醒
- 记录提醒触发结果

### 8.7 高风险动作审批

当 Agent Loop 需要执行高风险工具，例如删除文件、覆盖文件、执行命令，Neura 应该：

- 创建审批请求
- 暂停执行动作
- 等待用户 approve 或 reject
- 记录审批和执行结果

---

## 9. 核心流程

### 9.1 输入处理流程

```text
外部信息
↓
输入插件
↓
InputEvent
↓
Runtime
↓
Agent Loop
↓
标准化
↓
召回相关记忆
↓
模型分析
↓
决策
↓
记忆 / 提醒 / 输出 / 审批 / 日志
```

### 9.2 记忆沉淀流程

```text
标准化输入
↓
判断长期价值
↓
搜索相似记忆
↓
创建 / 更新 / 跳过
↓
记录来源和置信度
```

Neura 不应该把原始输入等同于记忆。

原始输入：

```text
我想做一个像 OpenClaw 一样的无 UI 智能体运行时。
```

可能形成的记忆：

```text
用户正在设计 Neura，一个无 UI 优先、常驻运行的个人上下文捕获与记忆运行时，核心结构包括输入插件、Agent Loop、记忆系统和输出插件。
```

### 9.3 输出流程

```text
任务结果 / 状态变化 / 定时任务 / 审批请求 / 用户查询
↓
输出决策
↓
OutputEvent
↓
输出路由
↓
输出插件
```

输入事件不必然产生输出。

输出事件也不必须来自某一次输入。提醒、状态变化、审批请求和定期回顾都可以独立产生输出。

---

## 10. 系统边界

### 10.1 Neura 是什么

Neura 是一个长期运行的个人智能体运行时，负责：

- 接收输入
- 标准化信息
- 调用模型分析
- 召回和写入记忆
- 调用受控工具
- 创建提醒和任务
- 路由输出
- 记录状态和日志

### 10.2 Neura 不是什么

Neura 不是：

- 传统 Web 服务端
- 传统桌面客户端
- 聊天机器人
- 纯 API 服务
- 传统笔记软件
- 通用自动化平台

### 10.3 UI 的位置

UI 不是 Neura 的本体。

UI 可以作为输出插件或管理面板存在，用于观察和控制 Runtime。

---

## 11. 系统架构

### 11.1 核心结构

```text
Input Plugins
↓
Runtime
↓
Agent Loop
↓
Memory / Tools / Schedule / Policy / Storage
↓
Output Routing
↓
Output Plugins
```

### 11.2 模块职责

- `Runtime`
  负责装配系统、管理生命周期、接收输入、调度插件、维护运行状态。

- `Agent Loop`
  负责处理单次输入或任务，完成标准化、记忆召回、模型分析、决策和结果落地。

- `Memory`
  负责长期记忆的写入、更新、检索和相似度判断。

- `Tools`
  负责执行内部能力，并接入权限策略和审批流程。

- `Policy`
  负责判断哪些动作允许自动执行，哪些动作需要确认。

- `Storage`
  负责持久化输入、任务、记忆、输出、插件、日志、审批和定时任务。

- `Input Plugins`
  负责把外部信息转换成 Neura 输入事件。

- `Output Plugins`
  负责把 Neura 输出事件发送给 CLI、日志、通知、管理界面或外部系统。

---

## 12. 插件体系

### 12.1 插件分类

插件只有两类：

```text
input
output
```

### 12.2 输入插件

输入插件负责：

- 监听外部信息
- 接收外部信息
- 转换为统一 InputEvent
- 发送给 Runtime

输入插件不负责：

- 长期记忆
- 复杂推理
- 输出结果
- 高风险工具调用

### 12.3 输出插件

输出插件负责：

- 接收 OutputEvent
- 转换为目标格式
- 发送到外部系统
- 记录输出结果

输出插件不负责：

- 采集输入
- 构建上下文
- 判断是否记忆
- 复杂推理

### 12.4 同一系统可以拆成输入和输出

例如消息应用：

- 用户发消息给 Neura：输入插件
- Neura 回复用户：输出插件

概念上仍然是两个方向，不引入第三种插件类型。

---

## 13. 功能需求

### 13.1 Runtime 管理

需要支持：

- 启动 Runtime
- 停止 Runtime
- 查看运行状态
- 查看运行时间
- 查看当前 Agent
- 查看插件状态
- 查看最近任务
- 查看错误日志

### 13.2 输入事件接收

输入事件类型：

- `text`
- `image`
- `file`
- `url`
- `event`
- `command`

输入事件字段：

- `id`
- `sourcePluginId`
- `type`
- `content`
- `metadata`
- `status`
- `createdAt`
- `processedAt`

状态：

- `pending`
- `processing`
- `completed`
- `failed`
- `ignored`

### 13.3 信息标准化

标准化结果应包含：

- 场景
- 统一文本内容
- 关键词
- 文件或图片描述
- 用户是否主动请求回应
- 是否像提醒
- 是否像偏好或长期决策
- 推荐的记忆搜索查询
- 初步任务类型

### 13.4 模型分析

模型应返回结构化分析结果，包括：

- 摘要
- 标签
- 任务类型
- 分类
- 意图
- 是否值得记忆
- 记忆类型
- 重要性
- 置信度
- 输出决策
- 输出原因
- 提取出的关键事实

### 13.5 记忆系统

记忆类型：

- 用户想法
- 项目设定
- 产品决策
- 技术决策
- 偏好
- 知识片段
- 任务线索
- 上下文总结

记忆字段：

- `id`
- `agentId`
- `content`
- `summary`
- `tags`
- `sourceInputId`
- `sourceType`
- `sourceId`
- `importance`
- `confidence`
- `createdAt`
- `updatedAt`

记忆规则：

- 不保存所有输入
- 优先保存长期上下文
- 重复或高度相似内容应更新已有记忆
- 临时信息可以只记录任务或日志
- 重要记忆应可被搜索和后续上下文召回

检索要求：

- 使用混合检索，而不是只依赖 `LIKE`
- 支持语义哈希向量、关键词、短语、标签、文本相似度、重要性和更新时间重排
- 中文查询需要 n-gram 增强
- CLI 和 Admin UI 应显示相关性分数和命中原因

大输入要求：

- 长文本和大文件必须预算化处理
- 支持预览、分块索引和 warning
- 二进制文档默认只存元信息
- URL 默认不自动抓正文

### 13.6 工具系统

基础工具：

- 搜索记忆
- 读取文件
- 写入文件
- 删除文件
- 执行命令
- 发送 HTTP 请求
- 调用模型

工具调用必须经过权限策略判断。

高风险工具默认不自动执行。

### 13.7 输出决策

输出产生条件：

- 用户主动查询
- CLI 输入需要即时反馈
- 提醒到期
- 出现错误
- 需要用户确认
- 发现重要信息
- 状态变化需要通知
- 外部系统要求响应

输出内容类型：

- 回答
- 摘要
- 提醒
- 确认请求
- 执行结果
- 错误提示
- 状态报告

### 13.8 插件管理

插件字段：

- `id`
- `name`
- `direction`
- `type`
- `status`
- `enabled`
- `config`
- `createdAt`
- `updatedAt`

插件状态：

- `enabled`
- `disabled`
- `running`
- `error`
- `waiting_config`

### 13.9 状态管理

Neura 需要能描述：

- Runtime 是否运行
- 当前 Agent
- 输入插件状态
- 输出插件状态
- 最近输入
- 最近任务
- 待审批动作
- 到期或未来定时任务
- 最近错误

### 13.10 日志系统

日志类型：

- Runtime 日志
- 输入日志
- 任务日志
- 模型调用日志
- 工具调用日志
- 记忆写入日志
- 输出日志
- 错误日志

---

## 14. 数据对象

### 14.1 Agent

- `id`
- `name`
- `status`
- `createdAt`
- `updatedAt`

### 14.2 Plugin

- `id`
- `name`
- `direction`
- `type`
- `status`
- `enabled`
- `config`
- `createdAt`
- `updatedAt`

### 14.3 InputEvent

- `id`
- `agentId`
- `pluginId`
- `type`
- `content`
- `metadata`
- `status`
- `createdAt`
- `processedAt`

### 14.4 Task

- `id`
- `agentId`
- `sourceType`
- `sourceId`
- `type`
- `status`
- `result`
- `error`
- `createdAt`
- `finishedAt`

`sourceType` 可以是：

- `input_event`
- `schedule`
- `runtime_state`
- `approval`
- `memory_review`
- `manual`
- `internal`

### 14.5 Memory

- `id`
- `agentId`
- `content`
- `summary`
- `tags`
- `sourceInputId`
- `sourceType`
- `sourceId`
- `importance`
- `confidence`
- `createdAt`
- `updatedAt`

### 14.6 OutputEvent

- `id`
- `agentId`
- `pluginId`
- `sourceType`
- `sourceId`
- `type`
- `content`
- `status`
- `createdAt`
- `sentAt`

### 14.7 ToolCall

- `id`
- `agentId`
- `taskId`
- `toolName`
- `input`
- `output`
- `status`
- `riskLevel`
- `approvalId`
- `createdAt`
- `finishedAt`

### 14.8 Approval

- `id`
- `agentId`
- `taskId`
- `actionType`
- `riskLevel`
- `payload`
- `status`
- `createdAt`
- `resolvedAt`

### 14.9 Schedule

- `id`
- `agentId`
- `mode`
- `content`
- `nextRunAt`
- `interval`
- `status`
- `createdAt`
- `updatedAt`

### 14.10 Log

- `id`
- `agentId`
- `level`
- `type`
- `message`
- `metadata`
- `createdAt`

---

## 15. 技术方向

### 15.1 推荐技术栈

- 语言：TypeScript
- 运行时：Bun
- 本地存储：SQLite
- CLI：Commander
- 配置：`neura.config.ts`
- 模型：远程模型 API 优先，同时保留 mock provider
- 插件：本地插件目录
- 部署：单机常驻进程

### 15.2 当前阶段不优先引入

- PostgreSQL
- Redis
- BullMQ
- Docker Compose
- NestJS
- Next.js 作为产品本体
- Tauri
- Electron
- Kubernetes

这些技术不是不能用，而是当前阶段会让系统重心偏离个人上下文闭环。

---

## 16. 推荐项目结构

```text
neura/
├── apps/
│   └── daemon/
├── bin/
├── packages/
│   ├── core/
│   ├── memory/
│   ├── model/
│   ├── storage/
│   ├── plugin-sdk/
│   ├── tools/
│   ├── policy/
│   └── shared/
├── plugins/
│   ├── inputs/
│   └── outputs/
├── data/
├── logs/
├── docs/
├── neura.config.ts
├── package.json
└── pnpm-lock.yaml
```

---

## 17. 非功能需求

### 17.1 长期运行

Runtime 应适合长期运行。单次任务失败不能导致整个 Runtime 崩溃。

### 17.2 可恢复

Runtime 异常退出后，再次启动时可以恢复状态，并继续处理可恢复任务。

### 17.3 可观测

用户可以看到：

- 当前状态
- 最近输入
- 最近任务
- 最近输出
- 记忆写入结果
- 错误日志
- 插件状态
- 待审批动作

### 17.4 安全

高风险工具调用必须受控。

默认策略应该偏保守，尤其是文件删除、文件覆盖、命令执行和外部系统写入。

### 17.5 可扩展

后续可以增加新的输入插件、输出插件和工具，但扩展必须服务于核心闭环。

---

## 18. 后续扩展方向

v1 稳定后，再考虑：

- 浏览器扩展输入
- 消息应用输入和输出
- 邮件输入和输出
- 周期性上下文回顾
- 更强的主题聚合
- 更好的记忆编辑和合并
- 多设备同步
- 插件安装和分发
- 与外部任务系统集成

---

## 19. 当前最重要的取舍

Neura 接下来最重要的不是继续扩大能力面，而是提高个人上下文闭环的质量。

优先级应是：

1. 输入是否足够顺手
2. 标准化是否稳定
3. 记忆判断是否可靠
4. 相似记忆更新是否避免重复
5. 搜索和回顾是否真的有用
6. 提醒和输出是否克制
7. 审批和权限是否让用户安心

可以暂缓的是：

1. 更多插件
2. 更复杂 UI
3. 多用户
4. 插件市场
5. 云同步
6. 通用自动化编排

---

## 20. 最终定义

Neura v1 的最终定义：

> Neura 是一个无 UI 优先、长期运行的个人上下文捕获与记忆运行时。它通过输入插件接收想法、截图、文件和外部事件，通过 Agent Loop 理解并沉淀长期记忆，并在需要时通过搜索、提醒、审批和输出插件把上下文重新交还给用户。

更短一点：

> Neura 是一个替你接住零散信息，并把它们变成长期上下文的个人智能体内核。
